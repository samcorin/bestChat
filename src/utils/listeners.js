

// ========= state.userList =========== Fetch user list and listen for new users (ALL USERS)
  // usersRef.on('value', snapshot => {
  //   console.log("usersRef.on('value', snapshot => ", snapshot.val());
  //   // NEW USER ADDED
  //   const filteredArr = objKeysToArray(snapshot.val()).filter((user) => user !== currentUser);
  //   store.dispatch(addUserList(filteredArr))
  // });
